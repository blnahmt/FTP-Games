import 'package:flutter/material.dart';

class LocalizationManager {
  static const en = Locale("en", "US");
  static const tr = Locale("tr", "TR");

  static const List<Locale> locales = [en, tr];
}
